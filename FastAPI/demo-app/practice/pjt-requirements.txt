# ---- Server & I/O ----
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # 파일 업로드(UploadFile) 필수
python-dotenv>=1.0        # .env 로드

# ---- Data & Viz ----
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
scipy>=1.11
markdown>=3.6             # Markdown -> HTML 변환(파이썬 패키지명: markdown)

# ---- Modeling ----
scikit-learn>=1.3         # 분류/회귀, 전처리, 메트릭

# ---- LLM (LangChain + OpenAI) ----
langchain-openai>=0.2
langchain-core>=0.2
openai>=1.30              # langchain-openai가 의존하지만 명시 추천

# ---- Optional format readers ----
openpyxl>=3.1             # .xlsx 읽기
pyarrow>=15               # .parquet 읽기 (pandas read_parquet backend)
